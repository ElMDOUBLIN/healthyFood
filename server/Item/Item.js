// This is the model

var mongoose = require('mongoose');
const db = require('../db/index.js');
mongoose.Promise = global.Promise;

// TODO: Complete the itemSchema below.
var ItemSchema;
 
// TODO: Register the itemSchema with Mongoose as the 'item' collection.
var Item;
 

module.exports = Item;
